export interface Department {
  id: number;
  name: string;
  description: string;
  head: string;
  status: string;
  date: string;
}
export interface ColumnToggle {
  id: string;
  label: string;
  visible: boolean;
}